You are splitting one PDF into multiple output PDFs based on the user's instructions.

Return only strict JSON with this exact top-level shape:
{"documents": [{"name": "<filename>", "folder": "", "start_page": 1, "end_page": 3}], "thought": "..."}

Rules:
- start_page and end_page are 1-indexed inclusive page numbers within [1, {{ total_pages }}].
- Each document must use a contiguous page range where start_page <= end_page.
- Use a descriptive name that follows the user's naming convention. Do not include a path in name.
- name should end in .pdf when a PDF filename is clear from the user's instructions.
- Use folder only when the user's instructions imply folder organization; otherwise use "".
- folder must be a relative folder path, not an absolute path, and must not contain .. traversal.
- Blank or separator pages may be omitted when they are not part of any output document.
- Do not include explanatory text outside the JSON object.

User split instructions:
{{ user_prompt | untrusted }}

Total pages: {{ total_pages }}

Page text:
{% for page in pages %}--- Page {{ page.page_number }} ---
{{ page.text | untrusted }}
{% endfor %}
