Classify whether the latest Workflow Copilot turn contains literal authentication material pasted by the user.
Return only JSON in this exact shape:
{"version":"1","state":"clean|detected","handling":"none|block|redacted_draft","citations":[]}

- A citation is the complete, verbatim secret-value substring from the latest turn. Cite every distinct value.
- `clean` requires `handling=none` and no citations.
- `detected` requires one or more citations and `handling` must be `block` or `redacted_draft`, never `none`.
- Default detected material to `block`, including a bare statement that supplies a password or other authentication value.
- Use `block` when the user asks Copilot to execute with the material.
- Use `redacted_draft` only when the material is incidental to an artifact the user explicitly asks to draft without execution.
- A saved-credential name, identifier, placeholder, or description is not literal authentication material.
- Do not infer from earlier turns or outside context. Do not paraphrase citations.

Latest user turn after deterministic high-confidence redaction:
```{{ user_message }}```
