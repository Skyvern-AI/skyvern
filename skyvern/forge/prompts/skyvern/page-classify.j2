You are classifying the current state of a web page. Given the current URL and page text, determine which of the provided options best describes the page state.

Current URL: `{{ current_url }}`

Page text (first 2000 characters):
```
{{ extracted_text }}
```

Classify the page into ONE of the following options:
{% for key, description in options.items() %}
- "{{ key }}": {{ description }}
{% endfor %}
- "UNKNOWN": None of the above options match the current page state.

Reply in JSON format:
{
    "reasoning": str,  // Brief explanation of why you chose this classification
    "classification": str,  // One of the option keys listed above, or "UNKNOWN"
    "confidence": float  // 0.0 to 1.0
}
