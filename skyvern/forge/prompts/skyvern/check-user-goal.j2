Your are here to help the user determine if the user has completed their goal on the web{{ " according to the complete criterion" if complete_criterion else "" }}. Use the content of the elements parsed from the page,{{ "" if without_screenshots else " the screenshots of the page," }} the user goal and user details to determine whether the {{ "complete criterion has been met" if complete_criterion else "user goal has been completed" }} or not.

Make sure to ONLY return the JSON object in this format with no additional text before or after it:
```json
{
{%- if slim_output not in ('safe', 'terse') %}
  "page_info": str, // Think step by step. Describe all the useful information in the page related to the user goal.{% endif %}
  "thoughts": str, // Think step by step. What information makes you believe whether user goal has completed or not. Use information you see on the site to explain.{{ " Maximum 15 words, telegraphic style — drop articles and filler words." if slim_output == 'terse' else "" }}
  "user_goal_achieved": bool // True if the user goal has been completed{{ " according to the complete criterion" if complete_criterion else "" }}, false otherwise.
}
```

User Goal:
```
{{ navigation_goal }}
```{% if big_goal_context %}

The user goal above is one step of a larger objective. The larger objective is provided ONLY as context to help you understand the step:
```
{{ big_goal_context }}
```
Certify ONLY the user goal above{{ " according to the complete criterion" if complete_criterion else "" }}: set user_goal_achieved to true as soon as that step is done, even if the larger objective is not finished yet. Do NOT require the larger objective to be complete. If the user goal describes an action and the Action History shows that action completed successfully, the step is done — set user_goal_achieved to true even if the acted-on element is no longer on the current page.{% endif %}

User Details:
```
{{ navigation_payload }}
```
{% if complete_criterion %}
Complete Criterion:
```
{{ complete_criterion }}
```{% endif %}
{% if action_history %}
Action History:
```
{{ action_history }}
```
{% endif %}{% if new_elements_ids %}
IDs for emerging HTML elements
```
{{ new_elements_ids }}
```
{% endif %}
Elements on the page:
```
{{ elements }}
```

Current datetime, ISO format:
```
{{ local_datetime }}
```
