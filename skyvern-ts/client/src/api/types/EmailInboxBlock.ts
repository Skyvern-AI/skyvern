// This file was auto-generated by Fern from our API Definition.

import type * as Skyvern from "../index.js";

export interface EmailInboxBlock {
    /** Author-facing identifier for a block; unique within a workflow. */
    label: string;
    /** Optional pointer to the next block label when constructing a DAG. Defaults to sequential order when omitted. */
    next_block_label?: string;
    output_parameter: Skyvern.OutputParameter;
    continue_on_failure?: boolean;
    model?: Record<string, unknown>;
    disable_cache?: boolean;
    ignore_workflow_system_prompt?: boolean;
    next_loop_on_failure?: boolean;
    email_client: EmailInboxBlock.EmailClient;
    credential_id?: string;
    folder?: string;
    prompt?: string;
    sender?: string;
    subject?: string;
    newer_than_days?: number;
    max_results?: number;
    include_body?: boolean;
    parameters?: Skyvern.EmailInboxBlockParametersItem[];
}

export namespace EmailInboxBlock {
    export const EmailClient = {
        Gmail: "gmail",
        Outlook: "outlook",
    } as const;
    export type EmailClient = (typeof EmailClient)[keyof typeof EmailClient];
}
