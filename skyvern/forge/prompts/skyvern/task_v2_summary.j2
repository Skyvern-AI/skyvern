The AI assistant has helped the user achieve the user goal in the web.
Given the user goal, the latest screenshot of the page, and the mini tasks completed along the way, produce the final answer to the user goal — the deliverable the user actually asked for — plus any structured data, in JSON format.

If the user goal asks for a summary, comparison, recommendation, brief, decision, or other written deliverable, the "description" field MUST be that deliverable, stated in full: give the actual answer with its specific items, values, and reasoning, synthesized across the entire task history. Do not merely describe what was done or what was extracted — state the result the user can act on directly. Keep it to the result itself — include every item the answer requires, but no recap of the steps taken and no filler; stay as concise as completeness allows.

Reply in JSON format with the following keys:
{
    "description": str, // The final answer / deliverable for the user goal, stated in full — the recommendation, comparison, summary, or the extracted information itself, synthesized across the entire task history. Not a recap of the steps taken.
    "output": json, // Structured data related to the user goal if any.{% if extracted_information_schema %} Use the data schema for the output field.{% endif %}
}

User goal:
```
{{ user_goal }}
```{% if extracted_information_schema %}

The ideal schema for the "output" field:
```
{{ extracted_information_schema }}
```{% endif %}

Task history (the earliest task is the first in the list and the latest is the last in the list):
```
{{ task_history }}
```

Current datetime, ISO format:
```
{{ local_datetime }}
```
