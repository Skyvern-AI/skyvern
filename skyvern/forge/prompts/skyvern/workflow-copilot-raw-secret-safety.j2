Classify whether the latest Workflow Copilot turn contains literal authentication material pasted by the user.
Return only JSON in this exact shape:
{"version":"1","state":"clean|detected","citations":[]}

- A citation is the complete, verbatim secret-value substring from the latest turn. Cite every distinct value.
- Cite each value in full. The server redacts exactly the spans you cite and keeps the rest of the turn, so a partial citation leaves the remainder of that value in place.
- `clean` requires no citations. `detected` requires one or more citations.
- A saved-credential name, identifier, placeholder, or description is not literal authentication material.
- First identify reusable account authentication. A value presented as a password is always reusable account authentication for this classification, including in a URL query or username/password URL userinfo. Reusable API keys and session credentials are also `detected` wherever they appear. An instruction to visit the URL never converts these credential types into scoped link capabilities. Cite the complete secret value, not the whole URL.
- Only after that check, treat a complete URL as operational destination input when the user explicitly directs the browser to open, navigate to, fetch from, or download from it and any remaining authentication-like material is a scoped capability conveyed by possession of that particular link (for example, a signed access link). Classify that delegated-destination turn as `clean`; cite only literal authentication material supplied separately from the destination.
- A URL merely mentioned as the source or location of authentication material is not a delegated browser destination. Authentication material supplied for use as authentication remains `detected`, including when the cited value appears inside such a URL.
- Do not infer from earlier turns or outside context. Do not paraphrase citations.

Latest user turn after deterministic high-confidence redaction:
```{{ user_message }}```
